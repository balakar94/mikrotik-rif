# ─────────────────────────────────────────────────────────────────────────────
# Rust
# ─────────────────────────────────────────────────────────────────────────────
/target/
**/target/
*.rs.bk
*.pdb
*.profraw
*.profdata
# NOTE: Cargo.lock is intentionally NOT ignored — this repository ships a binary.

# ─────────────────────────────────────────────────────────────────────────────
# Editors and IDEs
# ─────────────────────────────────────────────────────────────────────────────
.idea/
.vscode/
!.vscode/extensions.json
# Local agent tooling state (database, caches, installed packages).
.opencode/
*.swp
*.swo
*~

# ─────────────────────────────────────────────────────────────────────────────
# Operating-system cruft
# ─────────────────────────────────────────────────────────────────────────────
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db
Desktop.ini

# ─────────────────────────────────────────────────────────────────────────────
# Logs, temporary files and local scratch space
# ─────────────────────────────────────────────────────────────────────────────
*.log
/logs/
/tmp/
*.tmp

# ─────────────────────────────────────────────────────────────────────────────
# Packaged installers (cargo-packager / cargo-generate-rpm output)
# ─────────────────────────────────────────────────────────────────────────────
/dist/

# ─────────────────────────────────────────────────────────────────────────────
# Secrets and local environment
# ─────────────────────────────────────────────────────────────────────────────
.env
.env.*
!.env.example
*.pem
*.key

# ─────────────────────────────────────────────────────────────────────────────
# Local documentation dumps. These are copies of upstream MikroTik docs and
# must never be published: they are not part of the product and their licence
# is not ours to redistribute.
# ─────────────────────────────────────────────────────────────────────────────
llms.txt
llms-full.txt

# ─────────────────────────────────────────────────────────────────────────────
# RouterOS captures and exports. Real supout files can contain sensitive
# configuration, so they stay out of the history. Synthetic fixtures are the
# only exception.
# ─────────────────────────────────────────────────────────────────────────────
*.rif
*.rsc
!/tests/fixtures/**/*.rif
!/testdata/**/*.rif
/exports/
